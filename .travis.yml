language: java
sudo: false # faster builds
before_install:
- chmod +x mvnw
script:
# - "./mvnw clean install -B"
- "./mvnw clean org.jacoco:jacoco-maven-plugin:prepare-agent package sonar:sonar"
after_success:
- bash <(curl -s https://codecov.io/bash)
addons:
 sonarcloud:
   organization: springtraviscl
   token:
     secure: 7565bb3b8595d42ee172cd829812b0a1f06eb0a0